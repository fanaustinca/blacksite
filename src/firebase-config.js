// Firebase web-app config. This is safe to commit (web configs are public
// identifiers, not secrets — security lives in Firestore rules).
// Set to your project's config to enable accounts + co-op; null disables them.
export const FIREBASE_CONFIG = null;
